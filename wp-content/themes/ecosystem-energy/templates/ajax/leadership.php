<?php
// $context = Timber::context();

// global $params;
// $post_id = empty($params['post_id']) ? -1 : $params['post_id'];
// $leader_id = empty($params['id']) ? -1 : $params['id'];
// dd($post_id);
// $timber_post = new Timber\Post();
// // $blop = $timber_post->meta('leaders_list');
// // dd($blop);

// $response = Timber::compile('partials/leadership-panel.twig', $context);


// wp_reset_query();
// wp_reset_postdata();
// return wp_send_json($response);

// $posts = new Timber\PostQuery($args);

// if ($posts->found_posts > 0) {
//     $response = '';
//     $response .= Timber::compile('partials/perspectives-list.twig', ['posts' => $posts]);
//     $message = $response;
// } else {
//     $message = Timber::compile('partials/lists/no-results.twig');
// }
