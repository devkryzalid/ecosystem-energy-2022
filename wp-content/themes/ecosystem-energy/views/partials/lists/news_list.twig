{% block content %}

  {% if featured %}
    <div class="large container">
      {% include 'partials/block-item.twig' with { 
        title: featured.title,
        surtitle: featured.meta('related_industry')[0].post_title,
        footer: featured.post_date|date("F d, Y"),
        url: featured.link,
        image: featured.thumbnail,
        class: 'featured'
      } %}
    </div>
  {% endif %}

  <div class="large container">
	  <div class="block-list">
      {% for news in items %}
      
        {% include 'partials/block-item.twig' with { 
          title: news.title,
          surtitle: news.meta('related_industry')[0].post_title,
          footer: news.post_date|date("F d, Y"),
          url: news.link,
          image: news.thumbnail,
          class: 'text-padding'
        } %}

      {% endfor %}
	  </div>
	</div>

  {% include 'partials/lists/pagination.twig' with { 'elements': items.pagination(5) } %}

{% endblock %}
