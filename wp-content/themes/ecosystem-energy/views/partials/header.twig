<header id="header">

  {# SEARCH FIELD #}
  <div id="search-pane">
    <input type="text" placeholder="Search..." />
    <a id="search-close"><i class="icon-close-medium"></i></a>
  </div>

  {# COUNTRY SELECTOR #}
  <form id="country-pane" method="GET">
    <p>To read the appropriate content, please select your country :</p>

    <div class="countries">
    {{ dump(current_locale) }}
      <label for="country-all">
        <input id="country-all" type="radio" name="set_locale" value="-1" />
        All
      </label>

      {% for locale in locales %}
        <label for="country-{{ locale.term_id }}">
          <input id="country-{{ locale.term_id }}" type="radio" name="set_locale" value="{{ locale.term_id }}" />
          {{ locale.name }}
        </label>
      {% endfor %}
      
      {# <label for="country-canada">
        <input id="country-canada" type="radio" name="filter_locale" value="canada" />
        Canada
      </label>
      
      <label for="country-us">
        <input id="country-us" type="radio" name="filter_locale" value="us" />
        US
      </label> #}
    </div>

    <button type="submit" id="country-set" class="blue btn">Confirm</button>
    <a id="country-close"><i class="icon-close-medium"></i></a>
  </form>

  {# LOGO AND TOP NAV #}
  <div id="main-pane" class="show">
    <a id="logo" href="/">
      <img class="normal" src="{{ theme.link }}/assets/images/logos/logo-ecosystem.svg" alt="">
      <img class="invert" src="{{ theme.link }}/assets/images/logos/logo-ecosystem-invert.svg" alt="">
    </a>

    <nav id="top-menu">
      <a id="country-toggle"><i class="icon-position-pin"></i> CANADA</a>
      <a id="language-toggle">{{ current_lang | upper }}</a>
      <a id="search-toggle"><i class="icon-search"></i></a>
      <a id="menu-toggle">
        <span class="bar1"></span>
        <span class="bar2"></span>
      </a>
    </nav>
  </div>

  {# MAIN MENU #}
  {% include 'partials/menu.twig' with { items: menu_primary.items } %}

</header>